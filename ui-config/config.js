import { Dimensions } from 'react-native';

const { width, height } = Dimensions.get('screen');
const SPACING = 10;

class Colors {
  static primary = '#2B3595';
}

export { Colors, SPACING, width, height };
